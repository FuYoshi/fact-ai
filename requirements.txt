# FCGrad: Fair Cooperation in Mixed-Motive Games via Conflict-Aware Gradient Adjustment
# Reproducibility Study Requirements
#
# For GPU support (CUDA 12.x):
#   pip install -U "jax[cuda12]"
# For CPU-only:
#   pip install jax jaxlib

# Core JAX/ML dependencies
jax>=0.4.30
jaxlib>=0.4.30
chex==0.1.84
distrax==0.1.5
flax==0.7.4
numpy==1.26.4
optax==0.1.7
scipy==1.12.0

# Environment and RL dependencies
gymnax==0.0.8
jaxmarl==0.0.7
gymnasium==1.0.0
dm-env==1.6
dm-tree==0.1.8

# Configuration and logging
hydra-core==1.3.2
omegaconf>=2.3.0
wandb==0.18.5

# Visualization (for evaluation GIFs)
pillow>=9.0.0
matplotlib>=3.5.0
